server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/webwar_db?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: "@chen141!?ful@"
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 2MB
      max-request-size: 5MB
  web:
    resources:
      static-locations: classpath:/static/, file:D:/webwar-uploads/

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto

file:
  upload:
    dir: D:/webwar-uploads/